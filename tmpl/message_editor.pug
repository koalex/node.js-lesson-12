doctype html

html
    head
        meta(charset="utf-8")
        title= title
    body
        div(class="chat float-right")

            h4 Редактор сообщения
            form(action="/api/v1/messages/" + message._id method="POST" enctype="application/x-www-form-urlencoded")
                input(type="hidden" name="_method" value="put")
                textarea(name="message" cols="50" rows="3")= message.message
                br
                button(type="submit") СОХРАНИТЬ ИЗМЕНЕНИЯ
            br
            form(action="/api/v1/messages/" + message._id method="POST" enctype="application/x-www-form-urlencoded")
                input(type="hidden" name="_method" value="delete")
                button(type="submit" style="color:red;") УДАЛИТЬ СООБЩЕНИЕ
